# Database Configurations
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bloggers
    username: root
    password: Josh@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
      # other options , create, update, create-drop, validate
    show-sql: true

# Configs for custom port
#server:
#  port: